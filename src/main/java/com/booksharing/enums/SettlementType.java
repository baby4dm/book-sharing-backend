package com.booksharing.enums;

/**
 * Тип населеного пункту - місто, село чи селище. Використовується і в
 * профілі користувача (де він фактично живе), і опційно на самому
 * {@code Listing} (якщо книга/передача прив'язана до ІНШОГО місця, не
 * профілю власника - наприклад, власник тимчасово в іншому місті).
 */
public enum SettlementType {
    CITY,
    VILLAGE,
    SETTLEMENT
}